Reviewing the code manually.
The user reported an issue with a 401 error. My previous fix with `getHeaders()` inside the `PermissionService` was likely lost in a faulty revert, leading to requests without headers.
I've implemented a cleaner, global solution in Angular 17 style by providing a functional HttpInterceptor (`auth.interceptor.ts`) and wiring it into `app.config.ts`. Now, all requests to the API will intercept and inject the Bearer token if it exists in local storage.
I've ran the build, and there are no compilation issues.
