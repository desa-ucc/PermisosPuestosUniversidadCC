Realicé los cambios urgentes en el `AuthController.cs`:
- Eliminé el uso de `FromSqlRaw` y el subsecuente `.FirstOrDefault()` en memoria.
- Lo reemplacé por `_context.Database.SqlQueryRaw<UsuarioDto>("EXEC sp_Login @NombreUsuario={0}", request.Username).FirstOrDefaultAsync();`. Este enfoque obliga a EF Core a realizar un mapeo de resultados puro sin requerir que la entidad esté atada a un DbSet complejo, previniendo el congelamiento durante la validación de esquema o claves primarias.
- El proyecto compila correctamente y está listo.
