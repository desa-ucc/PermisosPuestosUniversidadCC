**1. Script SQL (`InitDB.sql` u objeto en motor)**
```sql
ALTER PROCEDURE sp_Getpt_SoftwareLocales
AS
BEGIN
    SELECT
        S.Id,
        S.EmpleadoId,
        ISNULL(E.NombreCompleto, 'Sin asignar') AS NombreEmpleado,
        S.GruposAD,
        S.NombreSoftware,
        S.Version,
        S.Fabricante
    FROM pt_SoftwareLocal S
    LEFT JOIN pt_Empleados E ON S.EmpleadoId = E.Id
END;
GO
```

**2. C# Backend (`PermisosPuestosApi/Models/Models.cs`)**
```csharp
public string? NombreEmpleado { get; set; }
```

**3. HTML Frontend (`software.component.ts`)**
```html
          <thead>
            <tr>
              <th>COLABORADOR</th>
              <th>Software</th>
              <th>Versión</th>
              <th>Fabricante</th>
              <th class="p-3 border-b border-gray-600 font-semibold text-center w-32">Acciones</th>
            </tr>
          </thead>
          <tbody>
            @for(sw of paginatedList; track sw.id) {
              <tr>
                <td>{{sw.nombreEmpleado || 'Sin asignar'}}</td>
                <td>{{sw.nombreSoftware}}</td>
                ...
```
