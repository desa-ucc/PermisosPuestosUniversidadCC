Fix Backend SQL Parameter and Serialization Mapping for BULK_UPDATE

- Configured the JSON serialization in `GuardarPermisos` inside `SeguridadController` to output camelCase matching the SP's `OPENJSON` paths.
- Added explicit Logging (`Console.WriteLine`) for the JSON Payload output.
- Modified the JSON SQL parameter ensuring large JSON payloads do not truncate using `System.Data.SqlDbType.NVarChar, -1`.
- Re-architected the `ExecuteSqlRawAsync` call string to strictly map parameters sequentially.
