refactor(docker): implement zero trust architecture and optimizations

- Multi-stage builds using alpine/git for both frontend and backend.
- Migrated frontend and backend to run as non-root `appuser`.
- Removed networking tools (`curl`, `wget`, etc.) for zero trust compliance.
- Grouped `RUN` instructions and implemented local caching cleanup for optimized image layers.
- Rewrote healthchecks in `docker-compose.yml` to use pure bash socket communication.
- Replaced hardcoded secrets with `.env` based configuration.
- Added extensive line-by-line documentation.
